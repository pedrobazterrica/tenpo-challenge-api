spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dockerdb
    username: dockeruser
    password: dockerpassword
  jpa:
    hibernate:
      ddl-auto: update
  redis:
    host: redis
    port: 6379
retry:
  maxAttempts: 3
  delay: 100

percentage:
  client:
    url: http://percentage-mock:8080/api/v1/percentage

server:
  port: 8090

